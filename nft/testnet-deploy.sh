# 1. Rename `config.json.sample` to `config.json`
# 2. In `config.json`, replace _SECRET_ with your actual recovery phrase.
# 3. Deploy a standard version of the Lighthouse NFT contract to testnet
lighthouse deploy
